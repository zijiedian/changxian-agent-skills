interface:
  display_name: "Changxian Remote Control"
  short_description: "Operate the bridge runtime and its persistent state"
  default_prompt: "Use $changxian-remote-control for bridge runtime work plus bridge-backed memory, role, and schedule state changes."
